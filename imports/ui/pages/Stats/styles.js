import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    statsContainer: {
      textAlign: "center"
    },
    liri: {
      width: "300px"
    }
  });

export default styles;
