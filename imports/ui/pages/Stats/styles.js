import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    statsContainer: {
      textAlign: "center",
      background: "white"
    },
    liri: {
      width: "300px"
    }
  });

export default styles;
