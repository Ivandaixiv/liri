import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    liri: {
        height: "800px",
        width: "400px",
    },
    container: {
    },
    loading: {
        width: "300px",
    },
    text: {
    }
  });
export default styles;