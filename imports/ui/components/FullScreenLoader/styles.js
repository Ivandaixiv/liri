import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    liri: {
        height: "450px",
        width: "500px",
        marginTop: "40vh"
    }
  });
export default styles;