import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    card: {
      width: "300px",
      height: "300px",
    },
    Container: {
        width: "300px"
    }
  });
export default styles;