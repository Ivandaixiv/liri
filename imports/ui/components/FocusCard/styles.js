import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    card: {
      width: "500px",
      height: "300px",
    },
    container: {
        width: "500px",
        margin: "5px"
    }
  });
export default styles;