import { createStyles } from "@material-ui/styles";

const styles = () =>
  createStyles({
    liri: {
      height: "100px",
      width: "50px",
      objectFit: "scale-down"
    }
  });

export default styles;
